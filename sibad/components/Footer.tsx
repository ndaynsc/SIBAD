import { FOOTER_TEXT } from '@/lib/constants';

export default function Footer() {
  return (
    <footer>
      <div className="container">
        <p>{FOOTER_TEXT}</p>
      </div>
    </footer>
  );
}
